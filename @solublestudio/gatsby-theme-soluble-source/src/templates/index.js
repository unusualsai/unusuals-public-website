import React from 'react';

import Layout from '../components/Layout';

export default function Index() {
    return (
        <Layout>
            <div style={{height: '100vh', padding: '50px'}}>
                Template not found. Check template name and create if not exist
            </div>
        </Layout>
    )
}
