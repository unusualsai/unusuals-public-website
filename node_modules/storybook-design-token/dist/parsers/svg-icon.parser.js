"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var SvgIconParser = /** @class */ (function () {
    function SvgIconParser() {
    }
    SvgIconParser.prototype.parse = function (tokenFiles) {
        return {
            tokenGroups: this.mapTokenFilesToTokenGroups(tokenFiles),
            keyframes: ''
        };
    };
    SvgIconParser.prototype.mapTokenFilesToTokenGroups = function (tokenFiles) {
        if (!tokenFiles || !tokenFiles.svgIcons) {
            return [];
        }
        return [
            {
                label: 'Icons',
                presenter: 'Svg',
                tokens: tokenFiles.svgIcons.map(function (file) {
                    var div = document.createElement('div');
                    div.innerHTML = file.content;
                    return {
                        editable: false,
                        key: div.querySelector('svg').getAttribute('data-token-name'),
                        value: file.content
                    };
                })
            }
        ];
    };
    return SvgIconParser;
}());
exports.SvgIconParser = SvgIconParser;
