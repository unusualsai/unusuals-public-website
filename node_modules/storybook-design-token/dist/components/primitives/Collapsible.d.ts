import * as React from 'react';
interface Props {
    children: React.ReactNode;
    id: string;
    title: string;
}
export declare const Collapsible: ({ children, id, title }: Props) => JSX.Element;
export {};
