"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
require("javascript-detect-element-resize");
var react_1 = require("react");
function usePanelWidth() {
    var _a = react_1.useState('narrow'), width = _a[0], setWidth = _a[1];
    react_1.useEffect(function () {
        var container = document.querySelector('.design-token-container');
        var callback = function () {
            var pxWidth = container.getBoundingClientRect().width;
            if (pxWidth < 500) {
                setWidth('narrow');
            }
            else if (pxWidth < 800) {
                setWidth('wide');
            }
            else {
                setWidth('wider');
            }
        };
        window.addResizeListener(container, callback);
        return function () {
            window.removeResizeListener(container, callback);
        };
    }, []);
    return width;
}
exports.usePanelWidth = usePanelWidth;
